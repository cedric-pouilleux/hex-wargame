// Central access point for DEFAULT_CONFIG.
// Story 1.3 will add: resources (satisfies ResourceDefinition[]) and buildings (satisfies BuildingDefinition[])
export { DEFAULT_CONFIG } from '../simulation/GameConfig'
